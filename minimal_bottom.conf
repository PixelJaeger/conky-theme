conky.config = {

    use_xft = true,
    xftalpha = 1,
    override_utf8_locale = true,
    short_units = true,

    update_interval = 1,
    total_run_times = 0,

    own_window = true,
    own_window_type = 'normal',
    own_window_transparent = false,
    own_window_argb_value = 2,
    own_window_argb_visual = true,
    own_window_colour = '000000',
    own_window_hints = 'undecorated,sticky,skip_taskbar,skip_pager',

    double_buffer = true,

    minimum_size = '500 5',
    maximum_width = 700,
    max_text_width = 0,

    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    draw_graph_borders = true,

    stippled_borders = 5,
    border_inner_margin = 0,
    border_outer_margin = 0,
    border_width = 1,

    default_color = '00FC0D',
    default_shade_color = 'black',
    default_outline_color = 'black',

    alignment = 'bottom_middle',

    gap_x = 1,
    gap_y = 1,

    no_buffers = true,
    uppercase = false,

    top_name_width = 18,
    top_cpu_separate = false,

    lua_load = 'zshrc-crawler.lua',
}


conky.text = [[
${font TerminessNerdFontMono-Regular:size= 11}|-- Aliases < ${lua conky_get_aliases}> ${alignr} |
|-- Plugins < ${lua conky_get_plugins}> ${alignr} |
|-- Theme < ${lua conky_get_powerlevel} >      ${alignr}< ${lua conky_get_oh_my_zsh}> Framework --|
@------------------------------------ ${alignc}zsh is da best shell! ${alignr}--------------------------------------@
]]
